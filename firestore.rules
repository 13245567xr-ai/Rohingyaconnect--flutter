rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FIXED PERMISSIONS ERROR: Allow auth users access to all app collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
